# Legacy tests

These tests cover the inactive Stage 3 PCA and Stage 4 clustering workflows.
They are retained with the corresponding implementations in
`scripts/event_features/old/`, but they are not part of the active test suite.

Do not restore them to normal test collection unless Stages 3 and 4 are
explicitly reactivated and their contracts are reviewed.
